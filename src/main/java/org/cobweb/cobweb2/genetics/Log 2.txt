/* Design log on Gene-linkage project
 * 
 * New classes:
 * 
 * 1) PhenotypeMaster
 * - Stores which phenotypes are linked to what genes
 * 
 * 
 * Modifications:
 * 
 * 1) Complex Agent:
 * - "setConstants()" method is changed to initialize a TreeMap() that stores mutable attributes
 *   and to modifiy the genetically-linked phenotypes according to the agent's genes
 * - TreeMap() global variable "mutables" is created to store mutable attributes
 * 
 * 2) GUI: 
 * - A new Genetic Algorithm tab is being added with its own JPanels and such
 * - JButton, JList, and JTable global instance variables are added
 * - The class now implements ActionListener and a actionPerformed() method is added
 * - Added writeHelperGA()
 * 
 * 3) Parser:
 * - Added the parsng of GA info